# 安全政策

## 支持范围

安全修复优先应用于最新的 `main` 分支。当前项目仍处于 V0.1 试验阶段，不承诺向后兼容。

## 报告漏洞

请使用 GitHub 仓库的 Private vulnerability reporting / Security Advisory 报告漏洞。不要创建包含漏洞细节、密钥、学生信息或可识别答题记录的公开 Issue。

报告中请包含受影响版本、复现步骤、实际影响和建议修复方式，但不要附带真实学生数据。

## 已知部署边界

- `.env*`、本地站点 ID 和临时启动文件均被 Git 忽略。
- 浏览器本地模式不适合共享设备或正式数据收集。
- `supabase/migrations/001_initial_schema.sql` 的匿名 RLS 策略只用于受控内测；公开部署前必须替换。
- 公开实例应配置日志留存期限、数据删除机制、滥用防护和最小权限访问。
